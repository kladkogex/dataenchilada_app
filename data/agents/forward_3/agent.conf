<source>
  @type forward

  bind 0.0.0.0
  port 24224

  linger_timeout 0
  
  
  log_level info
</source>



<match fluent.*>
  @type stdout
</match>

<match **>
  @type copy

  
  <store>
    
  @type cassandra_driver

  hosts 51.128.9.219

  keyspace data_enchilada
  column_family type_expected_forward_tag
  schema  '{:id => :integer, :name => :string, :created_at => :string, :updated_at => :string}'
  json_column json
  pop_data_keys false #????
  ttl 60


  </store>
  
  <store>
      @type webhdfs
  host 51.77.1.9
  port 50070
  path /data_enchilada/test2/%Y%m%d/data-%H.json
  flush_interval 10s
  <format>
    @type json
  </format>

  </store>
  
</match>
